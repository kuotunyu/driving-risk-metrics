__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
coverage.json
htmlcov/
*.egg-info/
build/
dist/
.venv/
venv/

# Datasets and local run outputs are never committed. The published evidence
# is tracked under docs/evidence/.
/data/
/artifacts/
/.tmp/
*.zip
*.tar.gz
CamVid/

# Model artefacts
*.pt
*.pth
*.onnx
checkpoints/

# Output paths of the pre-v1.0 layout. Nothing writes them any more; they stay
# ignored so stale outputs in an old local checkout are never staged.
reports/figures/
reports/report*.html

# Training artefacts
predictions/
runs/

!docs/assets/
/site/

# local agent working agreement (kept on disk, not published)
/AGENTS.md
